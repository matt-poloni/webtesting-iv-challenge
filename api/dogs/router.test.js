const router = require('./router.js');

describe('/dogs route', () => {
  it('should run tests', () => {
    expect(true).toBe(true);
  })

  describe.skip('POST method', () => {
    // it('should ')
  })
})